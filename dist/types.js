// ACP transport schema — framework-agnostic subset.
// Wire-format mirrors the internal monorepo definition at apps/api/src/routes/acp.ts;
// only the public-facing shape is re-declared here for OSS consumers.
export {};
